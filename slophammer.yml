rust:
  coverage:
    threshold: 85
    paths:
      - src
    exclude:
      - "target/**"
      - "tests/**"
      - "fixtures/**"
      - pattern: "captures/**"
        reason: recorded terminal captures and capture tooling, not shipped code
  complexity:
    cognitive_max: 8
  targets:
    - src
  exclude:
    - "target/**"
    - "tests/**"
    - "fixtures/**"
    - pattern: "captures/**"
      reason: recorded terminal captures and capture tooling, not shipped code
  dry:
    max_findings: 0
    paths:
      - src
    exclude:
      - pattern: "src/test_fixtures.rs"
        reason: shared test fixture data exercised only from tests
      - "**/*_test.rs"
      - "target/**"
      - "tests/**"
      - "fixtures/**"
      - pattern: "captures/**"
        reason: recorded terminal captures and capture tooling, not shipped code
    copied_blocks:
      enabled: true
      min_tokens: 100
  unsafe:
    policy: forbid
    allow:
      - path: "src/control/transport/windows.rs"
        reason: narrowly scoped Windows security-descriptor FFI required for owner-only named-pipe ACLs
  mutation:
    targets:
      - src/app
      - src/domain
      - src/fetch.rs
      - src/render
      - src/runner.rs
      - src/session.rs
  dependency_boundaries:
    - from: .
      allow: []
